<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pixelstat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pixelstat">
        <activity android:name=".StudentDetails"></activity>
        <activity android:name=".Studentdb" />
        <activity android:name=".Teacherdb" />
        <activity android:name=".StudentReg" />
        <activity android:name=".StudentLogin" />
        <activity android:name=".TeacherReg" />
        <activity android:name=".TeacherLogin" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>